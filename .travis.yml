language: python
python:
  - '3.7'
branches:
  only:
    - master
install:
  - pip install -r requirements.txt
script:
  - inv publish

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  local-dir: output
    on:
      branch: master